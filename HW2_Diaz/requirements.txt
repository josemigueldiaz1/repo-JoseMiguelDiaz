# HW2 — Accesibilidad geoespacial a establecimientos de salud resolutivos
# Probado con Python 3.12 en Windows 11.
#
#   python -m venv .venv
#   .venv\Scripts\activate        (Windows)   |   source .venv/bin/activate  (Linux/macOS)
#   pip install -r requirements.txt

# --- nucleo geoespacial ---------------------------------------------------------------
# geopandas >=1.0 es obligatorio: con 0.14.x + fiona >=1.10 la lectura de shapefiles
# revienta con "module 'fiona' has no attribute 'path'".
geopandas>=1.0,<2.0
pyogrio>=0.8            # motor de I/O vectorial (mucho mas rapido que fiona)
pyarrow>=15.0           # GeoParquet
shapely>=2.0
pyproj>=3.6
rtree>=1.0
rasterio>=1.3           # lectura del raster de poblacion WorldPop

# --- datos ----------------------------------------------------------------------------
pandas>=2.1
numpy>=1.26
scipy>=1.11             # cKDTree para el reparto de poblacion
scikit-learn>=1.4
requests>=2.31
tqdm>=4.66
chardet>=5.2            # deteccion de codificacion (regla R6)
openpyxl>=3.1
tabulate>=0.9           # tablas en Markdown / LaTeX
pypdf>=4.0              # comprobar el PDF del informe (paginas, texto, referencias)

# --- visualizacion --------------------------------------------------------------------
matplotlib>=3.8
mapclassify>=2.6        # esquemas de clasificacion para coropletas
contextily>=1.5         # mapas base en las figuras estaticas
folium>=0.15            # mapas interactivos
plotly>=5.18

# --- ruteo (Fase 2) -------------------------------------------------------------------
osmnx>=2.0
networkx>=3.2

# --- dashboard (Fase 4) ---------------------------------------------------------------
streamlit>=1.30
